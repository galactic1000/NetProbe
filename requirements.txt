# NetProbe runtime dependencies
# The scanner itself uses only the Python standard library — no packages required.

# Development / testing dependencies
pytest>=7.0
pytest-mock>=3.0
pytest-asyncio>=0.21
